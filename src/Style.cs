using System.Windows.Media;

namespace GeodeApp {
    static class Theme {
        public static class Colors {
            public static SolidColorBrush Background = new SolidColorBrush(Color.FromRgb(30, 30, 30));
        }
    }
}
